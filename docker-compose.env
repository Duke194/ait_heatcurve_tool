# Environment configuration for the Dash application

# Set to "development" for debugging, "production" for deployment
DASH_ENV=development

# The port on which the Dash application runs inside the container
PORT=8050

# Host setting (leave as 0.0.0.0 to allow external access)
HOST=0.0.0.0